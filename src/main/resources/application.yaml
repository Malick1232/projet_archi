spring:
  datasource:
    url: jdbc:mysql://localhost:3306/site_actualites
    username: site_user
    password: site_password

  jpa:
    hibernate:
      ddl-auto: create
    defer-datasource-initialization: true
    show-sql: true

  sql:
    init:
      mode: always